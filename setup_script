- Install arch ISO
  - Hyprland

- Install yay
- Install the packages in ./yay-packages.txt

git clone https://github.com/link1183/dotfiles ~/projects/github/dotfiles
./stowinit

git clone tpm in ~/.config/tmux/plugins/

ya pack -i

curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
